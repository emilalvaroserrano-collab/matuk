name: Build Translator Mode APK

on:
  push:
    branches: [main]
    paths:
      - 'Translator Mode/**'
      - '.github/workflows/build-translator-mode-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: translator-mode-release
  cancel-in-progress: true

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Show Flutter toolchain
        run: flutter --version

      - name: Verify production source configuration
        run: |
          grep -q 'HuggingFaceTB/SmolLM2-360M-Instruct-GGUF' "Translator Mode/lib/core/model_constants.dart"
          grep -q '48ab3034d0dd401fbc721eb1df3217902fee7dab9078992d66431f09b7750201' "Translator Mode/lib/core/model_constants.dart"
          grep -q '<|im_start|>assistant' "Translator Mode/lib/core/translation_prompt_renderer.dart"
          grep -q "Dual Translate" "Translator Mode/lib/screens/dual_translator_screen.dart"
          grep -q 'flutter_langdetect: ^0.0.2' "Translator Mode/pubspec.yaml"
          grep -q 'LocalLanguageRouter' "Translator Mode/lib/services/local_language_router.dart"
          grep -q 'latest detected non-Staff' "Translator Mode/lib/services/local_language_router.dart"
          grep -q 'autoDetectGuestLanguage = true' "Translator Mode/lib/controllers/translator_controller.dart"
          grep -q 'toggleVoiceSession' "Translator Mode/lib/controllers/translator_controller.dart"
          grep -q 'toggleMicMute' "Translator Mode/lib/controllers/translator_controller.dart"
          grep -q 'automatic language routing' "Translator Mode/lib/controllers/translator_controller.dart"
          grep -q 'lib_llama_cpp: ^0.7.3' "Translator Mode/pubspec.yaml"
          grep -q 'LlamaHttpServer.open' "Translator Mode/lib/services/local_translator_service.dart"
          grep -q 'libraryPath: Platform.isAndroid' "Translator Mode/lib/services/local_translator_service.dart"
          grep -q 'liblib_llama_cpp_android.so' "Translator Mode/lib/services/local_translator_service.dart"
          grep -q 'streamChatCompletion' "Translator Mode/lib/services/local_translator_service.dart"
          grep -q 'whisper_cpp_flutter_plus: ^0.4.1' "Translator Mode/pubspec.yaml"
          grep -q 'WhisperModelCatalog.base' "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q 'transcribeMicrophone' "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q 'updateInterval: Duration(milliseconds: 500)' "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q "_languageTag = 'auto'" "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q 'pauseListening' "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q 'playPcm16' "Translator Mode/lib/services/supertonic_tts_service.dart"
          grep -q 'audio_output' "Translator Mode/lib/services/supertonic_tts_service.dart"
          grep -q 'AudioTrack' "Translator Mode/scripts/apply_platform_config.py"
          grep -q 'version: 1.1.10+15' "Translator Mode/pubspec.yaml"
          if grep -R -n -E '@google/genai|google_generative_ai|generativelanguage\.googleapis\.com|google\.generativeai' "Translator Mode/lib" "Translator Mode/pubspec.yaml"; then
            echo 'ERROR: cloud generative-AI dependency/endpoint found in Translator Mode runtime'
            exit 1
          fi

      - name: Bootstrap Translator Mode
        working-directory: Translator Mode
        run: |
          chmod +x scripts/bootstrap_platforms.sh
          ./scripts/bootstrap_platforms.sh

      - name: Verify Android production host
        working-directory: Translator Mode
        run: |
          grep -q 'android:label="Dual Translate"' android/app/src/main/AndroidManifest.xml
          grep -q 'android:allowBackup="false"' android/app/src/main/AndroidManifest.xml
          grep -q 'android:usesCleartextTraffic="false"' android/app/src/main/AndroidManifest.xml
          grep -q 'android.permission.RECORD_AUDIO' android/app/src/main/AndroidManifest.xml
          grep -q 'AudioTrack' android/app/src/main/kotlin/ai/eburon/matuk_translator_mode/MainActivity.kt
          grep -q 'playPcm16' android/app/src/main/kotlin/ai/eburon/matuk_translator_mode/MainActivity.kt
          grep -q 'ai.eburon.dual_translate/audio_output' android/app/src/main/kotlin/ai/eburon/matuk_translator_mode/MainActivity.kt
          grep -q 'minSdk = 28' android/app/build.gradle.kts
          if grep -q 'android.speech.RecognitionService' android/app/src/main/AndroidManifest.xml; then
            echo 'ERROR: Android SpeechRecognizer bridge/query is still present'
            exit 1
          fi

      - name: Build release APK
        working-directory: Translator Mode
        run: flutter build apk --release

      - name: Verify APK native runtime
        working-directory: Translator Mode
        run: |
          APK=build/app/outputs/flutter-apk/app-release.apk
          test -f "$APK"
          ls -lh "$APK"
          sha256sum "$APK"
          unzip -l "$APK" | grep 'lib/arm64-v8a/libonnxruntime.so'
          if unzip -l "$APK" | grep -q 'lib/arm64-v8a/libonnxruntime4j_jni.so'; then
            echo 'ERROR: incompatible ONNX Runtime Java JNI bridge is still packaged'
            exit 1
          fi
          unzip -l "$APK" | grep -i 'whisper' >/dev/null || {
            echo 'ERROR: whisper.cpp native/runtime assets were not packaged'
            exit 1
          }
          unzip -l "$APK" | grep 'lib/arm64-v8a/liblib_llama_cpp_android.so' >/dev/null || {
            echo 'ERROR: isolated Eb Translator llama.cpp runtime is missing'
            exit 1
          }
          GGML_BASE_COUNT=$(unzip -l "$APK" | grep -c 'lib/arm64-v8a/libggml-base.so' || true)
          if [ "$GGML_BASE_COUNT" -ne 1 ]; then
            echo "ERROR: expected exactly one libggml-base.so for Whisper, found $GGML_BASE_COUNT"
            exit 1
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: dual-translate-v0.2.10-apk
          path: Translator Mode/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 30

      - name: Prepare release asset
        run: |
          cp "Translator Mode/build/app/outputs/flutter-apk/app-release.apk" dual-translate-v0.2.10.apk
          sha256sum dual-translate-v0.2.10.apk > dual-translate-v0.2.10.apk.sha256

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="translator-mode-v0.2.10"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release delete "$TAG" --yes --cleanup-tag
          fi
          gh release create "$TAG" \
            dual-translate-v0.2.10.apk \
            dual-translate-v0.2.10.apk.sha256 \
            --target "$GITHUB_SHA" \
            --title "Dual Translate v0.2.10" \
            --notes "Fixes Android Eb Translator startup failure: lib_llama_cpp_server defaults to libllama.so, while the Flutter Android plugin packages the isolated llama runtime as liblib_llama_cpp_android.so. The persistent local translator now explicitly opens the packaged Android library. Whisper.cpp sentence streaming, local language routing, warm-session translation flow, direct AudioTrack Speech Synthesys, and no-cloud inference remain unchanged."
