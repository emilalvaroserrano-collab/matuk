name: Build Translator Mode APK

on:
  push:
    branches: [main]
    paths:
      - 'Translator Mode/**'
      - '.github/workflows/build-translator-mode-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: translator-mode-release
  cancel-in-progress: true

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Show Flutter toolchain
        run: flutter --version

      - name: Verify production source configuration
        run: |
          grep -q 'HuggingFaceTB/SmolLM2-360M-Instruct-GGUF' "Translator Mode/lib/core/model_constants.dart"
          grep -q '48ab3034d0dd401fbc721eb1df3217902fee7dab9078992d66431f09b7750201' "Translator Mode/lib/core/model_constants.dart"
          grep -q '<|im_start|>assistant' "Translator Mode/lib/core/translation_prompt_renderer.dart"
          grep -q "Dual Translate" "Translator Mode/lib/screens/dual_translator_screen.dart"
          grep -q "installEbTranslator" "Translator Mode/lib/controllers/translator_controller.dart"
          grep -q "installSpeechRecognition" "Translator Mode/lib/controllers/translator_controller.dart"
          grep -q "installSpeechSynthesys" "Translator Mode/lib/controllers/translator_controller.dart"
          ! grep -q 'whisper_cpp_flutter_plus' "Translator Mode/pubspec.yaml"
          ! grep -q 'sherpa_asr_sdk' "Translator Mode/pubspec.yaml"
          grep -q 'MethodChannel' "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q 'on_device_stt' "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q 'setLanguageTag' "Translator Mode/lib/services/offline_stt_service.dart"
          grep -q 'sttCode' "Translator Mode/lib/models/translation_language.dart"
          grep -q 'createOnDeviceSpeechRecognizer' "Translator Mode/scripts/apply_platform_config.py"
          grep -q 'isOnDeviceRecognitionAvailable' "Translator Mode/scripts/apply_platform_config.py"
          grep -q 'version: 1.1.5+10' "Translator Mode/pubspec.yaml"

      - name: Bootstrap Translator Mode
        working-directory: Translator Mode
        run: |
          chmod +x scripts/bootstrap_platforms.sh
          ./scripts/bootstrap_platforms.sh

      - name: Verify Android production host
        working-directory: Translator Mode
        run: |
          grep -q 'android:label="Dual Translate"' android/app/src/main/AndroidManifest.xml
          grep -q 'android:allowBackup="false"' android/app/src/main/AndroidManifest.xml
          grep -q 'android:usesCleartextTraffic="false"' android/app/src/main/AndroidManifest.xml
          grep -q 'android.permission.RECORD_AUDIO' android/app/src/main/AndroidManifest.xml
          grep -q 'android.speech.RecognitionService' android/app/src/main/AndroidManifest.xml
          grep -q 'createOnDeviceSpeechRecognizer' android/app/src/main/kotlin/ai/eburon/matuk_translator_mode/MainActivity.kt
          grep -q 'EXTRA_PARTIAL_RESULTS' android/app/src/main/kotlin/ai/eburon/matuk_translator_mode/MainActivity.kt

      - name: Build release APK
        working-directory: Translator Mode
        run: flutter build apk --release

      - name: Verify APK native runtime
        working-directory: Translator Mode
        run: |
          APK=build/app/outputs/flutter-apk/app-release.apk
          test -f "$APK"
          ls -lh "$APK"
          sha256sum "$APK"
          unzip -l "$APK" | grep 'lib/arm64-v8a/libonnxruntime.so'
          if unzip -l "$APK" | grep -q 'lib/arm64-v8a/libonnxruntime4j_jni.so'; then
            echo 'ERROR: incompatible ONNX Runtime Java JNI bridge is still packaged'
            exit 1
          fi
          if unzip -l "$APK" | grep -qi 'whisper'; then
            echo 'ERROR: Whisper runtime is still packaged; Android built-in STT should require no STT model/runtime download'
            exit 1
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: dual-translate-v0.2.5-apk
          path: Translator Mode/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 30

      - name: Prepare release asset
        run: |
          cp "Translator Mode/build/app/outputs/flutter-apk/app-release.apk" dual-translate-v0.2.5.apk
          sha256sum dual-translate-v0.2.5.apk > dual-translate-v0.2.5.apk.sha256

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="translator-mode-v0.2.5"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release delete "$TAG" --yes --cleanup-tag
          fi
          gh release create "$TAG" \
            dual-translate-v0.2.5.apk \
            dual-translate-v0.2.5.apk.sha256 \
            --target "$GITHUB_SHA" \
            --title "Dual Translate v0.2.5" \
            --notes "Speech Recognition now uses Android's strict on-device SpeechRecognizer (API 31+) through a native Flutter platform channel. There is no downloadable STT model, no Whisper/Sherpa STT runtime, and no cloud fallback. The app checks SpeechRecognizer.isOnDeviceRecognitionAvailable, uses createOnDeviceSpeechRecognizer, enables partial results, and sends the selected language locale to Android. If the device does not provide an on-device recognizer, Speech Recognition is shown as unavailable rather than using network speech. Eb Translator and Speech Synthesys remain local and independently installable."
